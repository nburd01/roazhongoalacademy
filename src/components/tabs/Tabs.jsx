import React from 'react';
import PropTypes from 'prop-types';
import './tabs.scss';

/**
 * Primary UI component for user interaction
 */
export const Tabs = () => {
  
  return (
    <button
      
    >test
    </button>
  );
};
